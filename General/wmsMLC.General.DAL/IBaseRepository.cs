using System;

namespace wmsMLC.General.DAL
{
    /// <summary>
    /// Base repository interface. Nothing to do. 
    /// It's need only for constraint in code on work with repository
    /// All repositories must inherit this interface
    /// </summary>
    public interface IBaseRepository : IDisposable
    {
    }
}