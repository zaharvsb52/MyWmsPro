namespace wmsMLC.General.BL.Validation
{
    //NOTE: Не изменять порядок и не присваивать int значения. Критично, т.к. везде сохраняются как int.
    public enum ValidateErrorLevel
    {
        None,
        Information,
        Warning,
        Critical
    }
}
